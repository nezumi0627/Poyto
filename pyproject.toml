[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "poyto"
version = "0.1.0"
description = "Poyto — unofficial Python client for POYP, reconstructed from observed app traffic."
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
authors = [{ name = "nezumi0627" }]
keywords = ["poyp", "api", "client", "prediction-market"]
classifiers = [
  "Development Status :: 3 - Alpha",
  "License :: OSI Approved :: MIT License",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.10",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: 3.13",
  "Typing :: Typed",
]
dependencies = ["httpx>=0.27,<1"]

[project.optional-dependencies]
dev = [
  "pytest>=8,<9",
  "pytest-cov>=5,<7",
  "ruff>=0.12,<1",
  "mypy>=1.11,<2",
]

[project.scripts]
poyto = "poyto.cli:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
poyto = ["py.typed"]

[tool.pytest.ini_options]
addopts = "-q"
testpaths = ["tests"]

[tool.ruff]
target-version = "py310"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
ignore = ["E501", "E701", "E702", "UP035", "UP037"]

[tool.ruff.lint.per-file-ignores]
"examples/*.py" = ["I001"]

[tool.mypy]
python_version = "3.10"
warn_unused_configs = true
warn_return_any = false
check_untyped_defs = true
ignore_missing_imports = true
